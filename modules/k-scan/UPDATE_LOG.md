# Update Log

## 2026-02-03
- Moved the default prompt template into `prompts/default.txt` for easy user edits.
- Load the default template from file in `popup.js` and `background.js`, with a fallback.
- Preserve stored custom templates when present.
